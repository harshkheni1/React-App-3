import { FormInputField, Row, Col } from '../../../';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './input.stories';
import { Form } from 'react-uforms';

<Meta title="Form/Input" />

# Input field
<Preview withToolbar>
  <Story name="Field">{stories.InputField()}</Story>
</Preview>

# Input field size
<Preview>
  <Form
    onSubmit={() => {
      return;
    }}>
    <Row>
        <Col col={4}>
          <FormInputField label="Size SM" measure="sm" name="size_sm" mt={20} />
        </Col>
        <Col col={4}>
          <FormInputField label="Size MD" measure="md" name="size_md" mt={20} />
        </Col>
        <Col col={4}>
          <FormInputField label="Size LG" measure="lg" name="size_lg" mt={20} />
        </Col>
      </Row>
  </Form>
</Preview>

# Input field states
<Preview>
  <Form
    onSubmit={() => {
      return;
    }}>
    <Row>
        <Col col={4}>
          <FormInputField disabled label="Disabled" name="input_disabled" />
        </Col>
        <Col col={4}>
          <FormInputField className="is-success" label="Success" name="input_success" />
        </Col>
        <Col col={4}>
          <FormInputField className="is-invalid" required label="Error" name="input_error" />
        </Col>
      </Row>
  </Form>
</Preview>

# Input field props
<Props of={FormInputField} />
