import { styled } from '../../utils';
import { Button } from '../../';

export const ShowMoreStyled = styled('div')``;

export const ButtonTriggerStyled = styled(Button)`
  font-size: 14px;
  font-weight: normal;
  letter-spacing: normal;
  text-decoration: underline;

  &:hover {
    text-decoration: none;
  }
`;
